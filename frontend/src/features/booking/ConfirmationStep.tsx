import React from 'react';/** Displays persisted booking details returned by the API. */export function ConfirmationStep({booking}:{booking:{id:number;movie:{title:string};theatre:{name:string};seats:string[]}}):JSX.Element{return <section role="status"><h2>Congratulations!</h2><p>Booking #{booking.id}</p><p>{booking.movie.title} at {booking.theatre.name}</p><p>{booking.seats.join(', ')}</p></section>}
