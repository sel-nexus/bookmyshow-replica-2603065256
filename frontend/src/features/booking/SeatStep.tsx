import React,{useState} from 'react';
/** Shows the prescribed visual seat grid and preset-seat action. */ export function SeatStep({onSelect}:{onSelect:(seats:string[],price:number)=>void}):JSX.Element{const [selected,setSelected]=useState(false);return <section><h2>Select seats</h2><div aria-label="Seat grid">{['A1','A2','A3','A4','A5'].map(s=><span key={s} aria-label={s}>{selected&&['A1','A2','A3'].includes(s)?'●':'○'} {s} </span>)}</div><button onClick={()=>{setSelected(true);onSelect(['A1','A2','A3'],450)}}>Select Seats</button>{selected&&<p>Rs. 450 — A1, A2, A3</p>}</section>}
