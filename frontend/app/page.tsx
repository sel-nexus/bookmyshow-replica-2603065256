import { BookingFlow } from '../src/features/booking/BookingFlow';
/** Renders the public cinema-booking entry point. */
export default function HomePage(): JSX.Element { return <main><BookingFlow /></main>; }
